{
  "name": "eden-parfum-website",
  "version": "1.0.0",
  "description": "Full-stack Eden Parfum website with frontend and backend",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "start": "npm run backend:start",
    "simple-api": "node simple-server.js",
    "backend:dev": "cd backend && npm run dev",
    "backend:start": "cd backend && npm start",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:preview": "cd frontend && live-server --port=8080 --open=/index.html",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "echo 'Static site - copying frontend files for deployment'",
    "deploy:netlify": "echo 'Deploy to Netlify: Set publish directory to frontend/'",
    "deploy:github": "echo 'Deploy to GitHub Pages: Push to main branch with GitHub Actions'",
    "migrate:supabase": "cd backend && node ../database/migrate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "perfume",
    "website",
    "fullstack",
    "nodejs",
    "express"
  ],
  "author": "Eden Parfum",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "glob": "^11.0.3"
  }
}
